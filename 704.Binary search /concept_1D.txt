for agnostic binary search : we will have to check whether the given array is in ascending order or decending order 

for that we will compare the start and end element

boolean isAsc = arr[start] <arr[end];

while (s<=e){
            int mid = s + (e-s)/2 ;
            if (mid == target){
                 return mid ;}

            if (isAsc) {if(target < nums[mid]){

                e=mid-1;
            }else {

                s= mid+1;
            }}

else {if(target > nums[mid]){

                e=mid-1;
            }else {

                s= mid+1;
            }}        
        }

        return -1;

